server:
  port: 80

eureka:
  client:
    register-with-eureka: false
    fetch-registry: true    # 获取注册信息
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/

#开启
feign:
  hystrix:
    enabled: true